export default class Campaign {
  constructor(id, clientId, title, budget, description) {
    (this.id = id), 
    (this.clientId = clientId), 
    (this.title = title), 
    (this.budget = budget),
    (this.description = description);
  }
}
